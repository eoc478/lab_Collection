<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ 'style/style.css' | prefixedUrl }}">
  </head>
  <body>
    {% include "nav.njk" %}
    <main>{{ content | safe }}</main>
    {% for post in collections.post %}
      <li><a href="{{ post.url | prefixedUrl }}">{{ post.data.title }}</a> – {{ post.date }}</li>
    {% endfor %}
  </body>
</html>